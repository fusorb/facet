---
"@fusorb/facet-docs-site": patch
---

LiveCodePlayground ErrorBoundary: a demo snippet that throws now surfaces a readable message instead of hanging the React.Suspense page loader (the "lazy load" bug). QrScanner demo disabled autoStart so the preview does not request camera permission. PasswordStrengthMeter + AnnouncementBar demo wrappers supply fallback values / unique storageKeys so previews always render after a prior dismissal or undefined prop.
