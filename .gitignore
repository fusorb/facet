node_modules
dist
.next
.turbo
*.tsbuildinfo
.env
.env.local
.DS_Store
*.log
# Command Code local config (taste, settings) -- local only, not in git
# scratch / generated agent output
# .agent/ is local-only session tracking (todo/output dashboards, audit notes)
.commandcode
.agent/
test-out.txt
typecheck-out.txt
build-output.txt
typecheck-output.txt
trace-output.txt
ui_codebase_snapshot.txt
# Agent scratchpad (session-local, never committed)
scratchpad/

storybook-static