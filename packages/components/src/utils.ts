/**
 * Shared utilities re-exported from @fusorb/facet-utils (the canonical
 * owner: components → utils, motion → utils — no cycle, single source).
 */
export { cn, getModSymbol, isMac } from "@fusorb/facet-utils";
