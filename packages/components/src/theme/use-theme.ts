/**
 * @fusorb/facet-components: useTheme
 *
 * Re-exported so the hook is available from the root package entry:
 *   import { useTheme } from "@fusorb/facet-components";
 */

export { useTheme } from "./theme-provider.js";
