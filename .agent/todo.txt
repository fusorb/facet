facet - session plan (2026-09-13)
============================================================
Baseline (verified green): build + typecheck all packages; 739/739 tests.
Playground "errs": intentional `demo component exploded` caught by
  PlaygroundErrorBoundary (LiveCodePlayground.test.tsx, 16/16 pass). Not a real
  breakage.

TODOS:
[x] Baseline green (build/typecheck/739 tests)
[x] Confirm hidden-tooltip bug + radial hover + x-label/0 overlap
[x] Fix chart.tsx (working tree):
    - Remove `hidden`; clamp+offset tooltip (tooltipOffset prop) ✓
    - Band-scale xOf/catY so first/last category labels clear the "0" tick ✓
    - Radial hover: skip cartesian snap for radial (!isRadial) ✓
    - Radial per-slice legend (proper slice representation) ✓
    - Donut center: renderCenter prop + readable subtitle ✓
    - sliceLabelThreshold prop (hide tiny-slice labels) ✓
    - barRadius prop (replace hardcoded rx={4}) ✓
    - Horizontal bar: categories left / values bottom (hOf), correct orientation ✓
[x] Add chart.test.tsx coverage for new behavior (55→57 tests) ✓
[x] Fix 3 failing tests:
    - Origin-point regex: match comma-separated SVG coords (M 56,200) ✓
    - Crosshair test: find crosshair line by stroke-dasharray="4 2", not spine ✓
    - Manifest: add chart-range-selector entry + update CLAUDE.md count (113→114) ✓
[x] Crosshair: snap to nearest data point by default (crosshairSnap prop, default true)
    - crosshairX = crosshairSnap ? xOf(hover.dataIndex) : mousePos.x ✓
    - Vertical line + dot both snap to band-center xOf(hover.dataIndex) ✓
    - crosshairDotWidth/cx/cy all use snapped position ✓
    - crosshairPoints circles already snapped (xOf(hover.dataIndex)) ✓
    - Wrapped crosshair block in !isRadial (radial chart bleed fix) ✓
    - Test: crosshair snaps to data point (cx=128=xOf(0), cy=140=yOf(30)) ✓
    - Test: crosshairSnap={false} follows cursor (cx=100=mousePos.x) ✓
[x] Hardcoded values → configurable props + named constants:
    - lineWidth, dotRadius, dotInactiveOpacity (0.4→0.45 to match barInactiveOpacity) ✓
    - crosshairWidth, crosshairDasharray, crosshairOpacity, crosshairDotRadius,
      crosshairDotWidth ✓
    - axisWidth, gridlineWidth, gridlineDasharray, sliceWidth, legendInactiveOpacity ✓
    - DEFAULT_RADIAL_MARGIN, DEFAULT_CARTESIAN_MARGIN, DEFAULT_RADIAL_LABEL_GAP,
      DEFAULT_DONUT_INNER_RADIUS_RATIO, DEFAULT_TIP_WIDTH/HEIGHT ✓
    - DONUT_TOTAL_SCALE, DONUT_SUBTITLE_SCALE, PIE_SECONDARY_LABEL_SCALE ✓
    - DEFAULT_DOT_HOVER_SCALE, DEFAULT_SLICE_HOVER_SCALE, DEFAULT_BAR_HOVER_SCALE ✓
    - Fixed labelR bug (maxRadius + labelGap, was hardcoded 24) ✓
[x] Animation customization (transitionDuration + animationDuration fully wired):
    - animationDuration now drives fade-in CSS animation (was hardcoded 0.3s) ✓
    - transitionDuration now drives ALL hover transitions (was hardcoded 0.15s):
      dot radius/opacity/strokeWidth/transform, bar scale, slice hover, labels ✓
    - Tooltip transition uses transitionDuration (was Tailwind duration-150) ✓
    - crosshair-point tracking uses transitionDuration/3 (was hardcoded 0.05s) ✓
[x] Verify: components typecheck clean + chart tests 57/57 pass ✓
[x] Re-run CI gates (check:docs, manifest test) — all pass ✓
[x] README.md: component count aligned (111+ → 114) ✓
[x] Sweep stale artifacts: deleted .agent/image.png (was pre-existing session artifact,
    no longer needed after code-level diagnosis of crosshair drift) ✓
[x] Add EP 29 to episodes.md + episodes-plain.md (crosshair snap + animation wiring) ✓
[x] Add changeset (.changeset/chart-crosshair-snap.md) ✓
[ ] Update .agent/output.txt (status reflects snap + animation wiring)
[ ] Align CLAUDE.md: update stale claims (check:docs 90→114, test count, build date)
[ ] Verify full test suite (expect 745/745), docs drift, typecheck all green
[ ] Commit code changes (excluding PNGs) 